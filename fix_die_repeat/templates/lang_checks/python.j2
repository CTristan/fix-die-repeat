Python:
- [ ] No `eval()`/`exec()`/`compile()` with untrusted input
- [ ] No bare `except:` — catch specific exception types
- [ ] No mutable default arguments (e.g., `def f(x=[])`)
- [ ] Context managers (`with`) for file handles and resources
- [ ] No `assert` for runtime validation (stripped in `-O` mode)
- [ ] No string concatenation/interpolation for SQL or shell commands
